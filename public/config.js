// This file will be dynamically created by Choreo during deployment
// It will contain the configurations needed to connect to the backend API
window.configs = window.configs || {
  // Default configuration for local development
  apiUrl: 'http://localhost:8080'
};
